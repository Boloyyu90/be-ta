# ==================== SECURITY WARNING ====================
# This is an EXAMPLE file. DO NOT use these values in production!
# Copy this file to .env and replace ALL values with your own.
#
# Command: cp .env.example .env
#
# NEVER commit .env to git!
# ==================== SECURITY WARNING ====================

# ==================== SERVER CONFIGURATION ====================
NODE_ENV=development
# Menyesuaikan port default backend sekarang
PORT=3001

# ==================== DATABASE CONFIGURATION ====================
# Format: postgresql://USERNAME:PASSWORD@HOST:PORT/DATABASE?schema=SCHEMA
# Example (menyesuaikan struktur database skripsi):
#   postgresql://db_user:your_secure_password@localhost:5432/prestige_tryout_db?schema=app
#
# IMPORTANT: Replace with your actual database credentials
DATABASE_URL="postgresql://db_user:your_secure_password@localhost:5432/prestige_tryout_db?schema=app"

# ==================== CORS CONFIGURATION ====================
# Comma-separated list of allowed origins.
#
# Development (frontend di http://localhost:3000):
#   CORS_ORIGIN=http://localhost:3000
#
# Production (contoh):
#   CORS_ORIGIN=https://yourdomain.com,https://admin.yourdomain.com
#
CORS_ORIGIN=http://localhost:3000

# ==================== JWT CONFIGURATION ====================
# CRITICAL: Generate a strong random secret for production!
#
# Generate new secret (Linux/Mac):
#   openssl rand -hex 32
#
# Generate new secret (Node.js):
#   node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
#
# Generate new secret (Online):
#   https://randomkeygen.com/ (use "CodeIgniter Encryption Keys")
#
JWT_SECRET=CHANGE_THIS_TO_RANDOM_64_CHARACTER_HEX_STRING
JWT_ACCESS_EXPIRATION_MINUTES=30
JWT_REFRESH_EXPIRATION_DAYS=30

# ==================== EMAIL CONFIGURATION (OPTIONAL) ====================
# Uncomment and configure if you need email functionality
# For Gmail, generate App Password: https://myaccount.google.com/apppasswords
#
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your-email@gmail.com
# SMTP_PASSWORD=your-app-password

# ==================== ML/YOLO CONFIGURATION ====================
# Face detection proctoring settings

# Enable YOLO face detection (false = use mock analyzer for testing)
# Menyesuaikan default di .env (true), ubah ke false jika belum menyalakan YOLO service
YOLO_ENABLED=true

# Path to YOLO model file (required if YOLO_ENABLED=true)
# Download YOLOv8 face model from: https://github.com/akanametov/yolov8-face
YOLO_MODEL_PATH=./models/yolov8n-face.pt

# URL ke YOLO/ML service (Python backend)
# IMPORTANT for Windows: Use 127.0.0.1 instead of localhost to avoid IPv6 resolution issues
# Development default:
YOLO_SERVICE_URL=http://127.0.0.1:8000

# ML analysis timeout in milliseconds (default: 5000ms = 5 seconds)
ML_ANALYSIS_TIMEOUT_MS=5000

# Fallback to mock analyzer if YOLO fails (recommended: true for development)
ML_FALLBACK_TO_MOCK=true

# Warmup ML model on server startup
# Set to false untuk mempercepat restart saat development
ML_WARMUP_ON_STARTUP=true

# ==================== ADDITIONAL NOTES ====================
#
# 1. After copying this file to .env:
#    - Replace ALL placeholder values
#    - Generate NEW JWT_SECRET (NEVER use example value)
#    - Use STRONG database password
#    - Configure CORS for your actual domain
#
# 2. Security checklist:
#    ✅ .env file is in .gitignore
#    ✅ JWT_SECRET is unique and random
#    ✅ Database password is strong (16+ chars, mixed case, symbols)
#    ✅ CORS_ORIGIN is set for production
#
# 3. For production deployment:
#    - Use environment variables from hosting platform
#    - Enable HTTPS/TLS for database connections
#    - Use managed database service (e.g., AWS RDS, Railway)
#    - Enable database connection pooling
#    - Set up automated backups
#
# ==================== END ====================
