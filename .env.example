# Server
NODE_ENV=development
PORT=3000

# Database
DATABASE_URL="postgresql://prestige_user:boloPrestige9*@localhost:5432/prestige_tryout_db?schema=app"

# JWT
JWT_SECRET=ac6888abaed6b5339c2b9e6bd62af65774c458b8909b3ce30f03b23209516eb4
JWT_ACCESS_EXPIRATION_MINUTES=30
JWT_REFRESH_EXPIRATION_DAYS=30

# Email (optional for now)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your-email@gmail.com
# SMTP_PASSWORD=your-app-password

# ==================== ML/YOLO CONFIGURATION ====================
# Enable YOLO face detection (false = use mock analyzer)
YOLO_ENABLED=false

# Path to YOLO model file (required if YOLO_ENABLED=true)
YOLO_MODEL_PATH=./models/yolov8n-face.pt

# ML analysis timeout in milliseconds
ML_ANALYSIS_TIMEOUT_MS=5000

# Fallback to mock analyzer if YOLO fails (recommended: true)
ML_FALLBACK_TO_MOCK=true

# Warmup ML model on server startup (recommended: true)
ML_WARMUP_ON_STARTUP=true